# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Note: Copy this file to .env.local and fill in your actual Supabase credentials
# The SUPABASE_SERVICE_ROLE_KEY is required for the batch import API to work with admin permissions
# You can find these keys in your Supabase Dashboard under Settings > API

# Security Note: The service role key has elevated privileges and should NEVER be exposed 
# to the client-side. It's only used in server-side API routes for administrative operations.
